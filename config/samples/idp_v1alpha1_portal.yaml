apiVersion: idp.osura.dev/v1alpha1
kind: Portal
metadata:
  labels:
    app.kubernetes.io/name: lighthouse
    app.kubernetes.io/managed-by: kustomize
  name: portal-sample
spec:
  # TODO(user): Add fields here
